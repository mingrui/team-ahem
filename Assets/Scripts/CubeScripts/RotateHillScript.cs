using UnityEngine;
using System.Collections;

public class RotateHillScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
		transform.Rotate (0f, 0f, 180f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
