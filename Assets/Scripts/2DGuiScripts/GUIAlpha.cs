using UnityEngine;
using System.Collections;

public class GUIAlpha : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		guiTexture.color = new Color32(255, 255, 255, 100);
	
	}
}
